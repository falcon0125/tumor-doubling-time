
export interface MeasurementInputData {
  diameter: string;
  volume: string;
  time: string; // Will store date as "YYYY-MM-DD" string
}

export interface RiskProfile {
  label: string;
  color: string;
  textColor: string;
}